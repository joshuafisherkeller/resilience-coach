# Server-only. Never expose these values in the widget or commit a real .env.
OPENAI_API_KEY=
SUPABASE_URL=https://sftdtlrrkxavklyvixoo.supabase.co
# Preferred for new Supabase projects. The existing legacy service-role name
# remains supported during Supabase's migration window.
SUPABASE_SECRET_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Public HTTPS origin after deployment, with no trailing slash.
PUBLIC_BASE_URL=http://localhost:8787
PORT=8787
OPENAI_MODEL=gpt-5.6

# Optional shared secret for the demo reset endpoint/script.
DEMO_ADMIN_TOKEN=

# Local/offline testing only. Hosted builds must leave this at 0 and use Supabase.
DEMO_IN_MEMORY=0
